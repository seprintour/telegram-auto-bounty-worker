{
	"name": "telegram-auto-bounty-worker",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"start": "wrangler dev",
		"setup-key": "node ./deploy-keys.js"
	},
	"devDependencies": {
		"wrangler": "^3.0.0"
	}
}